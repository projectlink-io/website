const appContainer = {
  p: 4,
  mx: 'auto',
  maxW: 'container.lg',
}

const tabPanel = {
  ...appContainer,
  display: 'flex',
  py: '80px',
  alignItems: 'center',
}

const textColumn = {
  maxW: '480px',
}

const row = {
  py: '80px'
}

export default {
  appContainer,
  textColumn,
  tabPanel,
  row,
}
